<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/more"
        android:icon="@mipmap/ic_add"
        android:title="@string/str_more"
        app:showAsAction="always">
        <menu>
            <item xmlns:app="http://schemas.android.com/apk/res-auto"
                android:id="@+id/create"
                android:icon="@mipmap/ic_add"
                android:orderInCategory="100"
                android:title="@string/str_create"
                app:iconTint="@color/colorPrimary"
                app:showAsAction="always" />


            <item
                android:id="@+id/subscribe"
                android:icon="@mipmap/ic_subscribe_chanel"
                android:orderInCategory="101"
                android:title="@string/str_receiver"
                app:iconTint="@color/colorPrimary" />
        </menu>
    </item>
</menu>