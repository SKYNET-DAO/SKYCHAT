<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/search"
        android:icon="@mipmap/ic_search"
        android:title="@string/search"
        app:showAsAction="always" />

    <item
        android:id="@+id/more"
        android:icon="@mipmap/ic_add"
        android:title="@string/str_more"
        app:showAsAction="always"
        >
        <menu>
            <item
                android:id="@+id/chat"
                android:icon="@mipmap/ic_create_group_cheat"
                android:orderInCategory="100"
                android:title="@string/str_request_chat"
                app:iconTint="@color/colorPrimary"
                app:showAsAction="never" />

            <item
                android:id="@+id/add_contact"
                android:icon="@mipmap/ic_add"
                android:orderInCategory="101"
                android:title="@string/add_friend"
                app:iconTint="@color/colorPrimary" />
            <item
                android:id="@+id/scan_qrcode"
                android:icon="@mipmap/qr_code"
                android:orderInCategory="101"
                android:title="@string/str_scan_qrcode"
                app:iconTint="@color/colorPrimary" />
        </menu>
    </item>
</menu>